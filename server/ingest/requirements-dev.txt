# Test-only dependencies (NOT installed in the production image). The runtime image
# uses requirements.txt; tests additionally need pytest + httpx (FastAPI TestClient).
-r requirements.txt
pytest>=8
httpx>=0.28.1
